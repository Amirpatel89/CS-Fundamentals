import picasso.
import numpy.rand as r

def insertion_sort(a):
	for i in range (len(a)):
		temp = a[i]
		j = a[i]
		while j > 0 and [j - 1] > temp
			a[j] = a[j - 1]
			j -= 1
		a[j] = temp




a = [5, 4, 3, 2, 1]
print insertion_sort(a)




 for i in range(len(a)):
        temp = a[i]
        j = i
        while j > 0 and a[j - 1] > temp:
            a[j] = a[j - 1]
            j -= 1
        a[j] = temp

return a
